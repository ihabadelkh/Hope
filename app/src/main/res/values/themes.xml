<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Hope" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="textColor">
        <item name="android:textColor">@color/blue</item>
    </style>

    <style name="edtBackground">
        <item name="android:background">@drawable/edt_light_bg</item>
    </style>

    <style name="addFriendImageColor">
        <item name="tint">@color/light_gray</item>
    </style>

    <style name="tabIconTint">
        <item name="tabIconTint">@color/blue</item>
    </style>
</resources>